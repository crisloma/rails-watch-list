<%= simple_form_for([list, bookmark]) do |f| %>
  <%= f.input :movie_id, collection: Movie.all.collect { |m| [m.title, m.id] }, prompt: 'Select a movie' %>
  <%= f.input :comment %>
  <%= f.submit 'Create Bookmark', class: "btn btn-primary" %>
<% end %>