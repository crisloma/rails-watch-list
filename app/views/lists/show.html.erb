<h1>Show</h1>
  <h2><%= @list.name %></h2>
  <ul>
    <% @list.bookmarks.each do |bookmark| %>
    <li>
    <%= bookmark.movie.title %>
    <%= link to "Remove", bookmark_path(bookmark), method :delete %>
    </li>
    <% end %>
  </ul>
